<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="emailConfirmMapper">

    <!-- 이메일 체크 -->
    <select id="checkEmail" parameterType="Map" resultType="int">
        SELECT COUNT(*) AS COUNT
        FROM EMAIL_CONFIRM
        WHERE EMAIL = #{email}
    </select>



    <!--  이메일 체크 삽입  -->
    <insert id="insertEmail" parameterType="EmailConfirm">
        INSERT INTO EMAIL_CONFIRM (CONFIRM_NO, CODE, EMAIL, STATUS)
        VALUES(SEQ_CONFIRM.NEXTVAL, #{code}, #{email}, DEFAULT)
    </insert>

    <!-- 이메일 체크 삭제 -->
    <delete id="deleteAnother" parameterType="EmailConfirm" >
        delete EMAIL_CONFIRM
        where  EMAIl=#{email} and NOT CONFIRM_NO =#{confirmNo}
    </delete>




</mapper>