<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="holidayMapper">
    <insert id="insertMany" parameterType="List">
        insert all
        <foreach collection="list" item="Holiday" separator=",">
            into OFFICIAL_HOLIDAY (SEQ_HOLIDAY.NEXTVAL,#{Holiday.holiday},#{Holiday.holidayName})
        </foreach>

    </insert>
</mapper>